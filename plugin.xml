<?xml version="1.0" encoding="UTF-8"?>
<plugin xmlns="http://apache.org/cordova/ns/plugins/1.0" xmlns:android="http://schemas.android.com/apk/res/android"
        id="cordova-plugin-homescreen" version="0.2">
    
    <name>Cordova Kiosk Mode</name>
    <author>Jan Kalina</author>
    <description>Cordova plugin to use Cordova application "in kiosk mode" and as Android launcher</description>
    <keywords>cordova, launcher, homescreen, kiosk, kiosk mode</keywords>
    
    <engines>
        <engine name="cordova" version=">=3.0.0" />
    </engines>
    
    <platform name="android">
        
        <config-file target="AndroidManifest.xml" parent="/manifest/application">
            
            <!-- activity to use as launcher - because Cordova app is failing if started while boot -->
            <activity android:label="Cordova Kiosk app launcher" android:launchMode="singleTop" android:theme="@android:style/Theme.NoTitleBar.Fullscreen" android:name="jk.cordova.plugin.kiosk.HomeActivity">
                <intent-filter>
                    <action android:name="android.intent.action.MAIN" />
                    <category android:name="android.intent.category.DEFAULT" /><!-- Handler of app icon (required to be launcher) -->
                    <category android:name="android.intent.category.HOME" /><!-- Handler of Home button -->
                    <category android:name="android.intent.category.LAUNCHER" /><!-- Show in menu -->
                </intent-filter>
            </activity>
            
            <!-- inner activity, replacement of MainActivity with buttons blocked -->
            <activity android:label="Cordova Kiosk app" android:launchMode="singleTop" android:theme="@android:style/Theme.NoTitleBar.Fullscreen" android:name="jk.cordova.plugin.kiosk.KioskActivity">
            </activity>
            
        </config-file>
        
        <source-file src="HomeActivity.java" target-dir="src/jk/cordova/plugin/kiosk" />
        <source-file src="KioskActivity.java" target-dir="src/jk/cordova/plugin/kiosk" />
        
    </platform>
    
</plugin>
